if hexo-config('darkmode.enable') || hexo-config('display_mode') == 'dark'
  [data-theme='dark']
    --global-bg: darken(#3E444A, 2)
    --font-color: alpha(#FFFFFF, .7)
    --hr-border: alpha(#FFFFFF, .4)
    --hr-before-color: alpha(#FFFFFF, .7)
    --search-bg: #3E444A
    --search-input-color: alpha(#FFFFFF, .7)
    --search-result-title: alpha(#FFFFFF, .9)
    --preloader-bg: darken(#3E444A, 2)
    --preloader-color: alpha(#FFFFFF, .7)
    --tab-border-color: #60666C
    --tab-botton-bg: #60666C
    --tab-botton-color: alpha(#FFFFFF, .7)
    --tab-button-hover-bg: lighten(#3E444A, 15)
    --tab-button-active-bg: #3E444A
    --card-bg: lighten(#3E444A, 5)
    --sidebar-bg: #3E444A
    --btn-hover-color: lighten(#3E444A, 40)
    --btn-color: alpha(#FFFFFF, .7)
    --btn-bg: lighten(#3E444A, 10)
    --text-bg-hover: lighten(#3E444A, 15)
    --light-grey: alpha(#FFFFFF, .7)
    --white: alpha(#FFFFFF, .9)
    --text-highlight-color: alpha(#FFFFFF, .9)
    --blockquote-color: alpha(#FFFFFF, .7)
    --blockquote-bg: lighten(#3E444A, 10)
    --reward-pop: lighten(#3E444A, 10)
    --toc-link-color: alpha(#FFFFFF, .6)

    #web_bg:before,
    #footer:before,
    #page-header:before
      position: absolute
      width: 100%
      height: 100%
      background-color: alpha(#3E444A, .7)
      content: ''

    #article-container
      code
        background: #60666C

      pre > code
        background: 0

      .note
        code
          background: $code-background

      .aplayer
        filter: brightness(.8)

    // 頭部
    #page-header
      & > #nav.fixed,
      &.no-top-img #nav
        background: alpha(#3E444A, .8)
        box-shadow: 0 5px 6px -5px rgba(133, 133, 133, 0)

    // 代碼框
    #article-container
      pre,
      .highlight:not(.js-file-line-container)
        background-color: lighten(#3E444A, 2) !important
        color: alpha(#FFFFFF, .7) !important

      figure.highlight
        box-shadow: none

        if hexo-config('highlight_settings') && hexo-config('highlight_settings.enable')
          table::-webkit-scrollbar-thumb
            background: lighten(#3E444A, 5)

          .line:before
            color: alpha(#FFFFFF, .7) !important

          .hljs
            background-color: lighten(#3E444A, 2) !important

        pre[class*='language-']::-webkit-scrollbar-thumb
          background: lighten(#3E444A, 5)

        .highlight-tools
          background: lighten(#3E444A, 0) !important
          color: #90a4ae !important

    #post-comment
      #comment-switch
        if hexo-config('comments.text')
          background: #60666C !important

        .switch-btn
          filter: brightness(.8)

    // note
    if hexo-config('note.style') == 'modern' || hexo-config('note.style') == 'flat'
      .note
        filter: brightness(.8)

    // hide-tags
    .hide-button,
    .btn-beautify,
    .mermaid,
    .post-outdate-notice,
    .error-img,
    #article-container iframe,
    img,
    .gist
      filter: brightness(.8)

    #aside_content .aside-list > .aside-list-item:not(:last-child)
      border-bottom: 1px dashed alpha(#FFFFFF, .1)

    // 第三方
    // 插件 hexo-blog-encrypt
    #hexo-blog-encrypt
      label,
      input
        color: alpha(#FFFFFF, .7) !important

      input
        background-color: #3E444A

    // Gitalk
    #gitalk-container
      filter: brightness(.8)

      svg
        fill: alpha(#FFFFFF, .9) !important

    // Disqus and Disqusjs
    #disqus_thread
      #dsqjs
        .dsqjs-tab-active,
        .dsqjs-no-comment
          color: alpha(#FFFFFF, .7)

        .dsqjs-order-label
          background-color: lighten(#3E444A, 5)

        .dsqjs-post-body
          color: alpha(#FFFFFF, .7)

          code,
          pre
            background: #60666C

          blockquote
            color: alpha(#FFFFFF, .7)

    #artitalk_main #lazy
      background: #3E444A

    #operare_artitalk .c2
      background: #3E444A